import React from "react";
import { Text, View, StyleSheet } from "react-native";

const Componente = (props) => {
    return(
        <Text>Olá {props.nome}, tranquilidade mano?</Text>
    );
};

export default Componente;